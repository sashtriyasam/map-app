specVersion: "v1beta"
serviceId: "python"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "python-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
